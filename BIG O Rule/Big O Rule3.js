// Different terms(용어) for inputs⭐

function compressBoxesTwice(boxes, boxes2) {
  boxes.forEach(function (boxes) {
    console.log(boxes);
  });

  boxes2.forEach(function (boxes) {
    console.log(boxes);
  });
}

// O(2n) => O(n) => O(a + b)
// 만약 중첩함수일경우 O(a * b)
